# systemd-tmpfiles config for otzi-headless.
# Ensures /var/run/otzi exists on early boot (in case /var/run is tmpfs).
# RuntimeDirectory= in the systemd unit handles this when the unit starts;
# this entry is a belt-and-braces fallback for early boot consumers.
d /var/run/otzi 2770 root otzi -
